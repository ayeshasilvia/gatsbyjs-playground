import React from "react"
import Header from "../components/header"

export default () => (
  <div style={{ color: `teal` }}>
    <Header headerText="This is a head about Gatsby"/>
    <Header headerText="Saying something else"/>
    <p>Such wow. Very React.</p>
  </div>
)